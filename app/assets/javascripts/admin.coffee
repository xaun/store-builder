$(document).ready ->
  $('#products-show').tablesorter()

  # submitting admin dashboard views store select input
  # $('#dash-store-select').on('change', ->
  #   $('#dash-store-select').submit()
  # )

